# Optional ArcGIS Developer API key for Esri Wayback tiles.
# Anonymous requests work for hackathon-scale traffic. Set this only if you
# hit 429 rate-limit errors or want to raise the quota to 1M tile requests
# per month on the free ArcGIS Developer tier.
#
# How to obtain:
#   1. Create a free account at https://developers.arcgis.com/
#   2. Generate an API key with the "Basemap layers" scope
#   3. Copy it below and restart `npm run dev` (Next.js bakes
#      NEXT_PUBLIC_* env vars into the client bundle at build time).
#
# Leave empty (or unset) to keep anonymous Wayback access.
NEXT_PUBLIC_ARCGIS_API_KEY=

# Public base URL used for canonical, Open Graph, and Twitter image URLs.
# Set this in production so shared links resolve to the deployed Faro domain.
NEXT_PUBLIC_SITE_URL=

# Clerk authentication for private investigation folders and internal users.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/pais/AR?mode=investigations
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/pais/AR?mode=investigations

# Neon Postgres database for structured private product state.
DATABASE_URL=
FARO_ADMIN_EMAILS=
FARO_REVIEWER_EMAILS=

# Private Cloudflare R2-compatible storage for Aportes attachments.
# Example endpoint: https://<account-id>.r2.cloudflarestorage.com
STORAGE_ENDPOINT=
STORAGE_BUCKET=faro
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
MINIMAX_API_KEY=
INVESTIGATIONS_ACCESS_CODE=
